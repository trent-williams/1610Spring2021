using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LerpFunctions : MonoBehaviour
{
   //Code not meant to run. Ignore errors involving the variable "result" 
void Start()
/*{
    // In this case, result = 4
    float result = Mathf.Lerp(3f, 5f, 0.5f);
    
    //Additional tesing for color and vectors
    Vector3 from = new Vector3(1f, 2f, 3f);
    Vector3 to = new Vector3(5f, 6f, 7f);
    
    //Result
    Vector3 result = Vector3.Lerp(from, to,0.75f);
}

private void Update()
{
    light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f);
    //Adjusting code to play over time instead of with frame rate
    light.intensity = Mathf.Lerp(light.intensity, 8f, 0.5f * Time.deltaTime);
}*/
}
